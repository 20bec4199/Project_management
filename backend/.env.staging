# ── App ─────────────────────────────────────────────
NODE_ENV=staging
PORT=4000
FRONTEND_URL=https://your-staging-domain.com

# ── Database ─────────────────────────────────────────
DB_HOST=staging-db.internal
DB_PORT=5432
DB_USERNAME=app_staging
DB_PASSWORD=REPLACE_ME
DB_NAME=project_management
# ── Redis ────────────────────────────────────────────
REDIS_HOST=staging-redis.internal
REDIS_PORT=6379
REDIS_PASSWORD=REPLACE_ME

# ── JWT ──────────────────────────────────────────────
JWT_ACCESS_SECRET=REPLACE_ME_access_secret_staging
JWT_REFRESH_SECRET=REPLACE_ME_refresh_secret_staging
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=mtamilselvan9221@gmail.com
SMTP_PASS=sdbr ohdk oxcd xgva
SMTP_FROM=mtamilselvan9221@gmail.com
